# 时间口径正式交付与问题二、三复核

核查日期：2026-09-12。本次只完成问题一正式填表切换，并只读复核问题二、三；没有重新训练、优化或回放问题二、三。

## 1. 问题一：正式交付已切换

正式 `附件/附件5/result1.xlsx` 已从原始空模板重新填写为 `start_time_v1`：首列购电标签为 `0:10—0:20`，末两列为 `23:50—次日0:00` 和 `次日0:00—0:10`。144个计划量、12个充放电汇总值和2个端点值读回通过，模板样式和结构保持一致。正式费用35126.848589元，自然日0:00/24:00库存均为6000 kWh。

验证见 `results/q1_start_time_20260912/formal_delivery_validation.json`。

## 2. 问题二：实质映射和正式Excel已纠正

- 正式 `result2.xlsx` 首个模板时段为 `0:10—0:20`，最后时段为次日 `0:00—0:10`；
- 334日×144段共48096个计划购电量均已填写；
- 正式文件SHA256与 `results/q2_frozen_delivery/fill_validation.json` 一致，读回检查通过；
- `template_plan.csv` 全部决策的 `interval_start` 与“发布日期+10×(slot+1)分钟”一致，错位数为0；
- 自然日账本覆盖2025-02-01 00:00至2026-01-01 00:00之前，48096段连续且无重复无漏段；午夜执行前一日承诺。

因此问题二的**模型计算、自然/模板账本和正式交付表均已按新口径纠正**。

但发现两个仅属于导出元数据的遗留项，不影响求解、计费或正式Excel：

1. `results/q2_time_mapping/N_free/template_plan.csv::source_label` 的小时格式公式缺括号，slot 5—142显示小时错误；364个发布日共50232条展示标签不等于正确 `interval_start`。正式填表不读取该列，而是核对 `interval_start` 与原模板表头。
2. 已有报告登记的 `source_interval_mapping.csv` 电价行 `template_slot` 前143条导出为 `j-1`；模型数组和正式表仍直接使用附件存储顺序，价格值及费用正确。

这两项意味着问题二的时间元数据尚非完全洁净；冻结29号签名产物本次不改。后续若重生成29号产物，应修正展示字段并重新登记，不能静默覆盖旧签名结果。

## 3. 问题三：模型映射已纠正，正式表尚未填

- `code/q3_rolling_baseline_experiment.py` 已采用 `00:10→00:10—00:20`；
- 每日00:00只制定00:10起的新模板计划，当前00:00—00:10执行前日最后有效承诺；
- B2保存账本含正式自然日48096段及跨年尾段1段，共48097行，全部为连续10分钟区间；
- 自然日与模板账本分开，B2桥接恒等式残差约 `1.20e-9` 元；
- 正式 `result3.xlsx` 的原模板标签本身正确，但计划和调整购电量均仍为空，没有混入旧口径数值。

因此问题三的**建模与保存账本时间口径已经纠正**，但因Linear+q75仍只是候选，正式配置尚未冻结，`result3.xlsx` 尚未填写，不能称正式交付完成。

## 4. 证据

完整机器可读复核：`results/time_mapping_delivery_review_20260912/review.json`。复核脚本：`code/time_mapping_delivery_review.py`。
